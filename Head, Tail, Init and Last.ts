export const head = (x:Array<number>) => x[0];
export const tail = (x:Array<number>) => x.slice(1)
export const init = (x:Array<number>) => x.slice(0, -1)
export const last = (x:Array<number>) => x.slice(-1)[0]

